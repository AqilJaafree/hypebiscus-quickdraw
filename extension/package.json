{
  "name": "quickdraw-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/content.ts src/background.ts --bundle --outdir=dist --target=chrome110 --format=esm",
    "watch": "esbuild src/content.ts src/background.ts --bundle --outdir=dist --target=chrome110 --format=esm --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.3"
  }
}
